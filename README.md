```
Usage: version-sot [options]

Maintain version information across different configuration files

Options:
  -V, --version              output the version number
  -s, --sot <string>         define the source of truth (default: "package.json")
  -d --destination <string>  define the destination
  -h, --help                 display help for command
  ```